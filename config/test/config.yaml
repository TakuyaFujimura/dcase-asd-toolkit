defaults:
  - _self_
  - backend: null
  - experiments: null

extract: true
score: true
evaluate: true
umap: true
table_metric_list: ${hmean_list}


device: cuda:0 #cpu #
seed: 0
name: null
version: null
machine: null
ckpt_ver: null
result_dir: "./results"
tf32: true

batch_size: 64
num_workers: 0

hmean_list: []
umap_cfg: null


hydra:
  output_subdir: .hydra_test
  run:
    dir: ${result_dir}/${name}/${version}/${machine}
