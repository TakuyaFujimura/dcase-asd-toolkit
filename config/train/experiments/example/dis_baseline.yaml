# @package _global_
defaults:
    - override /datamodule: basic
    - override /model: multiresnet_1024
    - override /trainer: default


label_dict_path:
  main_label: "labels/${dcase}/machine_section_attr_domain.json"

model_ver: "all"
