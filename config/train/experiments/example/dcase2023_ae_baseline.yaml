# @package _global_
defaults:
    - override /datamodule: ae_flattenstft
    - override /model: ae_mlp
    - override /trainer: default

dcase: "dcase2023"

label_dict_path: {}


machine: "fan"

callback_opts:
  interval:
    every_n_epochs: 10
