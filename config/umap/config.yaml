defaults:
  - _self_
  - experiments: null



seed: null
dcase: null
name: null
version: null

infer_ver: null
result_dir: "./results"

machine: null

overwrite_embed: false
overwrite_image: false

metric: null
vis_type: null
embed_key: null

extract_items: []

trans_exec: true
trans_overwrite: false
vis_exec: true
vis_overwrite: false


hydra:
  output_subdir: .hydra_umap
  run:
    dir: ${result_dir}/${name}/${dcase}/${version}_${seed}/output/${infer_ver}/${machine}
